%% SPDX-License-Identifier: MIT
%% EUnit wrapper for LFE tests (run via rebar3 ct or eunit).

-module(aria_planner_SUITE).
-include_lib("common_test/include/ct.hrl").
-compile(export_all).

all() -> [].
